import React from 'react';
import AppPresenter from './AppPresenter';

const AppContainer = () => {
  /* Router */
  /* State */
  /* Functions */
  /* Hooks */
  /* Render */
  return <AppPresenter />;
};

export default AppContainer;
